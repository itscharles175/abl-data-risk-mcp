apiVersion: v1
kind: Service
metadata:
  name: abl-mcp
  namespace: abl-mcp
  labels:
    app.kubernetes.io/name: abl-mcp
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: abl-mcp
    app.kubernetes.io/component: server
  ports:
    - name: http
      port: 3333
      targetPort: http
      protocol: TCP
